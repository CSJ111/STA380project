% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_to_unique_patient.R
\name{filter_to_unique_patient}
\alias{filter_to_unique_patient}
\title{Filter to only one exam recorded per patient}
\usage{
filter_to_unique_patient(df, method = "latest", seed = 6)
}
\arguments{
\item{df}{a data frame returned by \code{load_ecg_data}.}

\item{method}{a string specifying which exam to keep per patient.
\code{"latest"} (default) keeps the exam with the numerically largest
exam_id; \code{"random"} keeps a randomly selected exam.}

\item{seed}{an integer random seed used only when \code{method = "random"}.
Default is 6.}
}
\value{
a data frame with exactly one row per unique patient_id.
}
\description{
Addresses the repeated measures / non-independence problem by
keeping only one exam recorded per patient. Because the dataset may contain
multiple exams for the same patient (the patient_id), using all rows would
violate the independence assumption required by the permutation test.
}
\examples{
df <- data.frame(
  patient_id = c(1, 1, 2, 2, 3),
  exam_id = c(101, 102, 201, 202, 301),
  age = c(50, 51, 60, 61, 70),
  nn_predicted_age = c(52, 53, 62, 63, 72),
  AF = c(TRUE, TRUE, FALSE, FALSE, TRUE)
)
df_one <- filter_to_unique_patient(df, method = "latest")
nrow(df_one) == length(unique(df$patient_id))
}
